export { renderFromUrl, renderFromFile } from './render';
export type { RenderOptions } from './render';

// Export renderProject function from the original app for backward compatibility
export { renderProject } from './legacy';